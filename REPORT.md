# 📋 Отчет о реализации проекта "Менеджер задач"

## 👤 Информация об авторе

- **Автор**: Филипп Рогозин
- **Дата создания**: 5 Января 2026

---

## 🎯 Цель работы

Разработать полнофункциональное веб-приложение для управления задачами, используя современные веб-технологии:
- **Frontend**: Vue 3 с Vue Router для маршрутизации
- **Backend**: FastAPI для REST API
- **DevOps**: Docker для контейнеризации приложения

Целью было получить практический опыт в:
- Разработке компонентного SPA приложения
- Работе с формами и валидацией
- API интеграции
- Docker контейнеризации
- Разработке REST API

---

## ✅ Реализованный функционал

### 📱 Frontend (Vue 3)

#### Созданные компоненты:
1. **AppHeader.vue** - Главное меню с навигацией
   - Логотип приложения
   - Ссылки навигации (Главная, Задачи)
   - Активные состояния для текущей страницы
   - Адаптивный дизайн

2. **AppFooter.vue** - Нижняя панель
   - Информация о авторских правах
   - Технологический стек
   - Стилизованный footer с градиентом

3. **LayoutCard.vue** - Компонент-контейнер с слотами
   - Default slot для основного содержимого
   - Named slot (#footer) для кнопок действий
   - Заголовок с градиентом
   - Сарок-ориентированный дизайн

4. **TaskList.vue** - Список задач
   - v-for для отображения всех задач
   - Фильтрация по статусу (все/выполненные/невыполненные)
   - Фильтрация по поиску
   - Сортировка по дате, названию, приоритету
   - Условный рендеринг пустого списка (v-if)
   - Передача событий к родителю

5. **TaskItem.vue** - Одна задача
   - Props для получения данных задачи
   - Визуальное отображение со статусом
   - Badges для приоритета и категории
   - Кнопки действия: редактировать, удалить, изменить статус
   - Checkboxes для отметки выполнения
   - Уникальное оформление для важных задач

6. **TaskForm.vue** - Форма создания/редактирования
   - Поле Заголовок (input с валидацией min 3 символа)
   - Поле Описание (textarea)
   - Выбор Приоритета (select: низкий/средний/высокий)
   - Выбор Категории (radio buttons: Работа/Личное/Учеба/Другое)
   - Чекбокс "Важно"
   - v-model с модификаторами (.trim)
   - Полная валидация на клиенте
   - Сообщения об успехе/ошибке
   - Отправка данных на сервер (создание/обновление)

7. **TaskFilters.vue** - Управление фильтрами
   - Select для фильтрации по статусу
   - Select для выбора сортировки
   - Input для поиска
   - Кнопка сброса фильтров
   - Реактивное обновление фильтров

#### Созданные Views (страницы):
1. **HomeView.vue** - Главная страница (`/`)
   - Приветственный экран
   - Информация о функциях приложения
   - Статистика (всего/выполнено/осталось задач)
   - Быстрые ссылки на основные действия

2. **TasksView.vue** - Список задач (`/tasks`)
   - Интеграция компонентов TaskList и TaskFilters
   - Загрузка и управление задачами
   - Обработчики для редактирования и удаления
   - Индикатор загрузки
   - Обработка ошибок

3. **CreateTaskView.vue** - Создание задачи (`/tasks/new`)
   - Использует TaskForm компонент
   - Перенаправление после успешного создания

4. **EditTaskView.vue** - Редактирование задачи (`/tasks/:id/edit`)
   - Загрузка задачи по ID из параметра маршрута
   - Использует TaskForm с инициальными данными
   - Обработка несуществующих задач (404)

5. **NotFoundView.vue** - Страница 404
   - Красивая страница для несуществующих маршрутов
   - Ссылки для навигации обратно

#### Маршрутизация (Vue Router):
- ✅ Простые маршруты: `/`, `/tasks`
- ✅ Параметризованные маршруты: `/tasks/:id/edit`
- ✅ Именованные маршруты: все маршруты имеют names
- ✅ Программная навигация: `$router.push()`
- ✅ Страница 404: `:pathMatch(.*)*`
- ✅ Активные ссылки: автоматически подсвечиваются

#### Реализованные техники:
- ✅ **v-for** - цикл для отображения списков
- ✅ **v-if/v-else** - условный рендеринг
- ✅ **v-model** - двусторонняя привязка данных
- ✅ **v-model.trim** - модификатор для обрезки пробелов
- ✅ **@click/@change/@submit** - обработчики событий
- ✅ **Computed properties** - вычисляемые свойства для фильтрации
- ✅ **Watched properties** - наблюдение за изменениями (watch)
- ✅ **Props** - передача данных от родителя к ребенку
- ✅ **Emit** - отправка событий от ребенка к родителю
- ✅ **Slots** - обычные и именованные слоты
- ✅ **Conditional classes** - условные классы `:class="{ active: isActive }"`
- ✅ **Lifecycle hooks** - mounted, watch

#### Стилизация:
- 🎨 Синяя и темно-синяя цветовая схема
- 📐 CSS Grid и Flexbox для разметки
- 📱 Полная адаптивность под мобильные устройства
- ✨ Плавные переходы и анимации
- 🎬 Hover эффекты и интерактивность
- 📊 Градиенты для глубины визуализации

### 🔙 Backend (Python FastAPI)

#### Реализованные API endpoints:

```
GET    /api/tasks          - Получить все задачи
GET    /api/tasks/{id}     - Получить одну задачу
POST   /api/tasks          - Создать новую задачу
PUT    /api/tasks/{id}     - Обновить существующую задачу
DELETE /api/tasks/{id}     - Удалить задачу
GET    /api/stats          - Получить статистику
```

#### Модели данных (Pydantic):

**Task Model**
- `id`: int - уникальный идентификатор
- `title`: str (min 3, max 200 символов) - название задачи
- `description`: Optional[str] - описание
- `priority`: enum (low/medium/high) - приоритет
- `category`: str (Работа/Личное/Учеба/Другое) - категория
- `important`: bool - отмечена как важная
- `completed`: bool - выполнена ли задача
- `created_at`: datetime - время создания
- `updated_at`: datetime - время последнего обновления

#### Валидация данных:
- ✅ Pydantic валидация на входе
- ✅ Проверка длины заголовка (3-200 символов)
- ✅ Проверка допустимых категорий
- ✅ Проверка приоритета (enum)
- ✅ Обработка ошибок 404 для несуществующих задач
- ✅ Автоматические timestamps (created_at, updated_at)

#### Хранение данных:
- 📁 JSON файл: `backend/tasks.json`
- 💾 Сохранение при каждом изменении
- 🔄 Читаемый JSON формат (pretty print)
- ✅ Инициализация файла при запуске

#### Функциональность:
- ✅ CRUD операции
- ✅ Автоматическое генерирование ID
- ✅ Управление временными метками
- ✅ Статистика задач
- ✅ CORS для работы с фронтенд приложением

### 🐳 Docker

#### Frontend Dockerfile:
- Multi-stage build для оптимизации размера
- Stage 1: Сборка приложения с npm
- Stage 2: Запуск через http-server
- Expose port 5000

#### Backend Dockerfile:
- Python 3.11 alpine base image
- Установка зависимостей из requirements.txt
- Запуск uvicorn сервера
- Expose port 8000

#### docker-compose.yml:
- Frontend сервис (порт 5000)
- Backend сервис (порт 8000)
- Общая сеть (app-network)
- Том для данных (tasks.json)
- Автоматический перезапуск

---

## 📸 Интерфейс приложения

### Главная страница `/`
```
┌─────────────────────────────────────────┐
│        📋 Менеджер задач               │
├─────────────────────────────────────────┤
│  Добро пожаловать в Менеджер задач    │
│  Организуйте свои задачи эффективно    │
│                                         │
│  ✅ Простое управление                 │
│  🔍 Фильтрация и сортировка            │
│  🎯 Приоритизация                      │
│  📊 Статистика                         │
│                                         │
│  Всего: 5 | Выполнено: 2 | Осталось: 3│
│                                         │
│  [→ Перейти к задачам] [➕ Новая]     │
└─────────────────────────────────────────┘
```

### Список задач `/tasks`
```
┌─────────────────────────────────────────┐
│ Статус: [все▼] Сортировка: [дата▼]   │
│ Поиск: [____________] [🔄 Сбросить]   │
├─────────────────────────────────────────┤
│ ☑ Завершить проект Vue 3    ⭐ Высокий │
│   Реализовать полный функционал...      │
│   15 января 2024 | [✏️] [🗑️]           │
│                                         │
│ ☐ Настроить Docker          Высокий    │
│   Создать контейнеры для проекта        │
│   10 января 2024 | [✏️] [🗑️]           │
└─────────────────────────────────────────┘
```

### Форма создания `/tasks/new`
```
┌─────────────────────────────────────────┐
│  Создание/Редактирование задачи        │
├─────────────────────────────────────────┤
│ Заголовок: [___________________]        │
│ Описание:  [________________...]        │
│ Приоритет: [Средний▼]                   │
│ Категория: ○ Работа ○ Личное           │
│            ○ Учеба ○ Другое            │
│ ☐ Отметить как важное                  │
│                                         │
│              [➕ Создать] [← Отмена]   │
└─────────────────────────────────────────┘
```

---

## 💻 Код примеры

### Фильтрация и сортировка задач
```javascript
computed: {
  filteredTasks() {
    let result = [...this.tasks]
    
    // Фильтрация по статусу
    if (this.filters.status === 'completed') {
      result = result.filter(t => t.completed)
    } else if (this.filters.status === 'pending') {
      result = result.filter(t => !t.completed)
    }
    
    // Фильтрация по поиску
    if (this.filters.search) {
      const search = this.filters.search.toLowerCase()
      result = result.filter(t =>
        t.title.toLowerCase().includes(search) ||
        (t.description && t.description.toLowerCase().includes(search))
      )
    }
    
    // Сортировка
    switch (this.filters.sort) {
      case 'date-asc':
        result.sort((a, b) => new Date(a.created_at) - new Date(b.created_at))
        break
      case 'title-asc':
        result.sort((a, b) => a.title.localeCompare(b.title, 'ru'))
        break
      // ... другие варианты сортировки
    }
    
    return result
  }
}
```

### Использование компонента сProps и Events
```vue
<TaskItem
  v-for="task in filteredTasks"
  :key="task.id"
  :task="task"
  @toggle="toggleTask($event)"
  @edit="editTask($event)"
  @delete="deleteTask($event)"
/>
```

### Компонент с слотами
```vue
<LayoutCard title="Управление задачами">
  <!-- Default slot -->
  <div class="task-list">
    <TaskList :tasks="tasks" @update="onUpdate" />
  </div>
  
  <!-- Named slot -->
  <template #footer>
    <router-link to="/tasks/new" class="btn btn-primary">
      ➕ Новая задача
    </router-link>
  </template>
</LayoutCard>
```

### API сервис (Axios)
```javascript
async getTasks() {
  try {
    const response = await api.get('/tasks')
    return response.data
  } catch (error) {
    throw new Error(error.response?.data?.detail || error.message)
  }
}

async createTask(taskData) {
  try {
    const response = await api.post('/tasks', taskData)
    return response.data
  } catch (error) {
    throw new Error(error.response?.data?.detail || error.message)
  }
}
```

### FastAPI эндпоинт
```python
@app.post("/api/tasks", response_model=Task, status_code=status.HTTP_201_CREATED)
async def create_task(task_create: TaskCreate):
    """Создать новую задачу"""
    tasks_data = read_tasks()
    
    task_dict = task_create.dict()
    task_dict['id'] = get_next_id(tasks_data)
    task_dict['completed'] = False
    task_dict['created_at'] = datetime.now().isoformat()
    task_dict['updated_at'] = datetime.now().isoformat()
    
    tasks_data.append(task_dict)
    write_tasks(tasks_data)
    
    return task_dict
```

---

## 📊 Пример JSON данных

```json
[
  {
    "id": 1,
    "title": "Завершить проект Vue 3",
    "description": "Реализовать полный функционал менеджера задач на Vue 3",
    "priority": "high",
    "category": "Работа",
    "important": true,
    "completed": true,
    "created_at": "2024-01-01T10:00:00",
    "updated_at": "2024-01-05T14:30:00"
  },
  {
    "id": 2,
    "title": "Настроить Docker контейнеры",
    "description": "Создать Dockerfile для фронтенда и бэкенда",
    "priority": "high",
    "category": "Работа",
    "important": true,
    "completed": false,
    "created_at": "2024-01-02T09:15:00",
    "updated_at": "2024-01-02T09:15:00"
  }
]
```

---

## 🚀 Инструкция по запуску

### С Docker (рекомендуется)

```bash
# 1. Перейти в директорию проекта
cd vuedemo

# 2. Собрать Docker образы
docker-compose build

# 3. Запустить контейнеры
docker-compose up

# 4. Открыть в браузере
# Frontend: http://localhost:5000
# Backend: http://localhost:8000
```

### Локальный запуск

#### Frontend
```bash
cd frontend
npm install
npm run dev
# Откройте http://localhost:5173
```

#### Backend
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python main.py
# Сервер на http://localhost:8000
```

---

## 🎓 Выводы

### Что было изучено:

1. **Vue 3 Основы**
   - Компонентная архитектура
   - Реактивность (refs, computed, watch)
   - Жизненный цикл компонентов
   - Управление состоянием на уровне компонентов

2. **Vue Router 4**
   - Настройка маршрутов
   - Параметризованные маршруты
   - Программная навигация
   - Активные ссылки

3. **Работа с формами**
   - v-model с модификаторами
   - Валидация данных на клиенте
   - Обработка событий формы
   - Отправка данных на сервер

4. **API интеграция**
   - Axios для HTTP запросов
   - Обработка ошибок
   - Асинхронные операции

5. **Vue компоненты**
   - Props для передачи данных
   - Events (emit) для отправки событий от ребенка
   - Слоты (default, named, scoped)
   - Компонентная композиция

6. **FastAPI**
   - Создание REST API
   - Pydantic для валидации
   - Path parameters и query parameters
   - CRUD операции
   - Status codes и обработка ошибок

7. **Хранение данных**
   - Работа с JSON файлами
   - Сохранение состояния
   - Синхронизация данных

8. **Docker и DevOps**
   - Создание Dockerfile
   - Multi-stage builds
   - docker-compose для оркестрации
   - Работа с портами и сетями контейнеров

9. **CSS и дизайн**
   - CSS Grid и Flexbox
   - Адаптивный дизайн (media queries)
   - Градиенты и тени
   - Анимации и переходы
   - Современная цветовая схема

10. **Архитектура SPA**
    - Разделение на компоненты
    - Разделение на views (страницы)
    - Services для API
    - Router для навигации

### Ключевые достижения:
✅ Полнофункциональное приложение для управления задачами  
✅ Красивый и интуитивный интерфейс  
✅ Полная валидация на клиенте и сервере  
✅ Docker контейнеризация  
✅ REST API с CRUD операциями  
✅ Адаптивный дизайн  
✅ Профессиональный код структурирование  

---

## 📊 Использованные технологии

| Категория | Технология | Версия |
|-----------|-----------|--------|
| **Frontend** | Vue.js | 3.4.0 |
| | Vue Router | 4.3.0 |
| | Axios | 1.6.0 |
| | Vite | 5.0.0 |
| | CSS3 | - |
| **Backend** | Python | 3.11 |
| | FastAPI | 0.104.1 |
| | Pydantic | 2.5.0 |
| | Uvicorn | 0.24.0 |
| **DevOps** | Docker | Latest |
| | Docker Compose | Latest |

---

## 📁 Структура финального проекта

```
vuedemo/
├── frontend/                      # Фронтенд приложение
│   ├── src/
│   │   ├── components/            # Компоненты
│   │   │   ├── AppHeader.vue
│   │   │   ├── AppFooter.vue
│   │   │   ├── LayoutCard.vue
│   │   │   ├── TaskList.vue
│   │   │   ├── TaskItem.vue
│   │   │   ├── TaskForm.vue
│   │   │   └── TaskFilters.vue
│   │   ├── views/                 # Страницы
│   │   │   ├── HomeView.vue
│   │   │   ├── TasksView.vue
│   │   │   ├── CreateTaskView.vue
│   │   │   ├── EditTaskView.vue
│   │   │   └── NotFoundView.vue
│   │   ├── router/
│   │   │   └── index.js           # Конфигурация маршрутов
│   │   ├── services/
│   │   │   └── api.js             # API сервис
│   │   ├── App.vue
│   │   ├── main.js
│   │   └── styles.css
│   ├── public/
│   │   └── index.html
│   ├── package.json
│   ├── vite.config.js
│   └── Dockerfile
│
├── backend/                       # Бэкенд API
│   ├── main.py                    # Основное приложение FastAPI
│   ├── models.py                  # Pydantic модели
│   ├── tasks.json                 # Хранилище данных
│   ├── requirements.txt
│   ├── Dockerfile
│   └── .gitignore
│
├── docker-compose.yml             # Оркестрация контейнеров
├── .gitignore
├── README_PROJECT.md              # Документация проекта
└── REPORT.md                      # Этот файл
```

---

## 🎉 Заключение

Проект "Менеджер задач" успешно реализован со всеми требуемыми функциями. Приложение демонстрирует:

- ✅ Глубокое понимание Vue 3 экосистемы
- ✅ Навыки создания REST API с FastAPI
- ✅ Опыт контейнеризации приложений с Docker
- ✅ Хорошие практики в организации кода
- ✅ Внимание к пользовательскому опыту и дизайну

Приложение полностью функционально, протестировано и готово к использованию. Код хорошо организован, документирован и следует лучшим практикам разработки.

**Статус**: 🎉 **ЗАВЕРШЕНО** 🎉

---

*Отчет подготовлен: Январь 2024*  
*Версия: 1.0.0*
